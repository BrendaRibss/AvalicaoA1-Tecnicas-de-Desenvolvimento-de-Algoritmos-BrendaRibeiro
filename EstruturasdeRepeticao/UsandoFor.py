# Exemplo usando FOR. 
 
#O For cria um Loop que percorre os números de 1 até 100.
for num in range(1, 101):

    # Verifica se o número é par (resto da divisão por 2 é zero)
    if num % 2 == 0:
        
        print(num)
